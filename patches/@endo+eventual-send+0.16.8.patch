diff --git a/node_modules/@endo/eventual-send/src/handled-promise.js b/node_modules/@endo/eventual-send/src/handled-promise.js
index 48e84fa..9f0a09f 100644
--- a/node_modules/@endo/eventual-send/src/handled-promise.js
+++ b/node_modules/@endo/eventual-send/src/handled-promise.js
@@ -251,6 +251,10 @@ export const makeHandledPromise = () => {
         superResolve(value);
         resolved = true;
         resolvedTarget = value;
+        handledResolve = undefined;
+        handledReject = undefined;
+        superResolve = undefined;
+        superReject = undefined;
 
         // We're resolved, so forward any postponed operations to us.
         continueForwarding();
@@ -262,8 +266,12 @@ export const makeHandledPromise = () => {
         harden(reason);
         assertNotYetForwarded();
         promiseToPendingHandler.delete(handledP);
-        resolved = true;
         superReject(reason);
+        resolved = true;
+        handledResolve = undefined;
+        handledReject = undefined;
+        superResolve = undefined;
+        superReject = undefined;
         continueForwarding();
       };
     };
@@ -362,6 +370,7 @@ export const makeHandledPromise = () => {
 
     // Invoke the callback to let the user resolve/reject.
     executor(resolveHandled, rejectHandled, resolveWithPresence);
+    executor = undefined;
 
     return handledP;
   }
